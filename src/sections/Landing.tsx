import { Flex, Text } from '@chakra-ui/react';

export const Landing = () => {
  return (
    <Flex
      h="100vh"
      id="Landing"
      as="section"
      w="full"
      alignItems={'center'}
      justifyContent={'center'}
    >
      <Text>Pledge</Text>
    </Flex>
  );
};
